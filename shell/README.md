# Static Frost shell

The Quickshell composition added in Phase 3 will be first-party and static. This directory may not contain a plugin registry, externally loadable QML, a notification server, a PAM lock implementation, or a Polkit agent.

