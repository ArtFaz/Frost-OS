post_install() {
  cat <<'EOF'
frost-settings installed. To apply the first-boot reconciliation
(hardened Hyprlock PAM, snapper config, firewall), enable the unit:

  systemctl enable frost-firstboot.service

bootstrap-cachyos does this for a clean install.
EOF
}

post_upgrade() {
  :
}
