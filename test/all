#!/bin/bash

set -euo pipefail

root=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)

for script in "$root"/tools/* "$root"/test/*; do
  [[ -f $script ]] && bash -n "$script"
done
"$root/tools/validate-package-config"
"$root/test/package-build"
"$root/test/signatures"
