# Key material required

The build tool requires `frost.gpg`, `frost-trusted`, and `frost-revoked` in this directory. Only public material belongs here. Creating the real signing key and exporting these files requires the explicit approval described in `docs/key-bootstrap.md`.

