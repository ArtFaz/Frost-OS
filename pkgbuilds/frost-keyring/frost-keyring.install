post_install() {
  if [[ -x usr/bin/pacman-key ]]; then
    usr/bin/pacman-key --populate frost
  fi
}

post_upgrade() {
  post_install
}

post_remove() {
  if [[ -x usr/bin/pacman-key ]]; then
    usr/bin/pacman-key --updatedb
  fi
}

